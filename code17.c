#include<stdio.h>
int main()
{
    int num;
    printf("Enter the number:");
    scanf("%d",&num);
    if (num<0)
    {
        /* code */printf("it is a negative number");
    }
    else if (num>0)
    {
        /* code */printf("it is a positive number");
    }
    else if (num==0)
    {
        /* code */printf("the number is zero");
    }
    
    
    
}